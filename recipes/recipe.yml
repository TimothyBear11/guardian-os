# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: guardian-os
description: Final Guardian Workstation (ROCm + Ollama + Terra Fallback)
base-image: quay.io/fedora-ostree-desktops/cosmic-atomic
image-version: 44

modules:
  # STEP 1: Repo Setup & The "404 Bypass"
  - type: script
    snippets:
      - |
        set -ouex pipefail
        # 1. Install RPM Fusion properly
        rpm-ostree install \
            https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-44.noarch.rpm \
            https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-44.noarch.rpm

        # 2. THE FIX: Force Terra to use F43 until F44 is ready
        cat << 'EOF' > /etc/yum.repos.d/terra.repo
        [terra]
        name=Terra Fallback (F43)
        baseurl=https://repos.fyralabs.com/terra43/$basearch/
        enabled=1
        gpgcheck=0
        # Give permission for these specific packages to be seen
        includepkgs=noctalia-qs,noctalia-shell,quickshell,scx-scheds,scx-tools
        EOF

  # STEP 2: The DNF Solver (Now with working repos)
  - type: dnf
    repos:
      files:
        - https://negativo17.org/repos/fedora-steam.repo
      copr:
        - yalter/niri
        - lionheartp/Hyprland
        - lilay/topgrade
        - avengemedia/dms
    install:
      install-weak-deps: false
      packages:
        # Gaming & GPU (RX 6700 XT)
        - steam
        - rocm-hip
        - rocm-opencl
        - rocm-runtime
        - rocm-smi
        - clinfo
        - nvtop
        # 32-bit Doodoo
        - mesa-dri-drivers.i686
        - mesa-vulkan-drivers.i686
        - vulkan-loader.i686
        - glibc.i686
        - ibus-gtk2.i686
        # Performance & Desktop (Found via Terra F43 + COPR)
        - niri
        - hyprland
        - qtile-wayland
        - scx-scheds
        - scx-tools
        - dms
        - topgrade
        # Den Essentials
        - fish
        - neovim
        - emacs-nox
        - fastfetch
        - btop
        - plymouth-plugin-script

  # STEP 3: Assets, AI, & System Services
  - type: script
    snippets:
      # Sched-ext: Rusty 5800X Optimization
      - |
        cat << 'EOF' > /etc/systemd/system/scx_rusty.service
        [Unit]
        Description=Sched-ext Rusty Scheduler
        After=multi-user.target
        [Service]
        Type=simple
        ExecStart=/usr/bin/scx_rusty
        Restart=always
        [Install]
        WantedBy=multi-user.target
        EOF
        systemctl enable scx_rusty.service

      # Ollama Engine Setup
      - |
        curl -L https://ollama.com/download/ollama-linux-amd64.tar.gz | tar -xz -C /usr
        cat << 'EOF' > /etc/systemd/system/ollama.service
        [Unit]
        Description=Ollama Service
        After=network-online.target
        [Service]
        ExecStart=/usr/bin/ollama serve
        User=root
        Restart=always
        Environment="OLLAMA_HOST=0.0.0.0"
        [Install]
        WantedBy=multi-user.target
        EOF
        systemctl enable ollama.service

      # Noctalia Shell Assets (Jail Logic)
      - |
        mkdir -p /usr/lib/noctalia
        dnf download --destdir=/tmp -y noctalia-qs --arch=$(arch)
        rpm2cpio /tmp/noctalia-qs*.rpm | cpio -idmv -D /usr/lib/noctalia
        mkdir -p /usr/lib/noctalia/usr/share/quickshell/shells/noctalia
        curl -sL https://github.com/noctalia-dev/noctalia-shell/releases/latest/download/noctalia-latest.tar.gz | \
        tar -xz --strip-components=1 -C /usr/lib/noctalia/usr/share/quickshell/shells/noctalia
        # Create Wrapper
        cat << 'EOF' > /usr/bin/noctalia-qs
        #!/bin/bash
        export QML2_IMPORT_PATH=/usr/lib/noctalia/usr/lib64/qt6/qml:$QML2_IMPORT_PATH
        export QUICKSHELL_SHELL_PATH=/usr/lib/noctalia/usr/share/quickshell/shells
        if [[ "$*" == *"-c"* ]] || [[ "$*" == *"--config"* ]]; then
            exec /usr/lib/noctalia/usr/bin/qs "$@"
        else
            exec /usr/lib/noctalia/usr/bin/qs -c noctalia "$@"
        fi
        EOF
        chmod +x /usr/bin/noctalia-qs

  # STEP 4: Configuration & Dots
  - type: chezmoi
    repository: "https://github.com/timothybear11/ArtixDots"
    all-users: true

  - type: brew

  # STEP 5: Branding
  - type: os-release
    properties:
      NAME: Guardian OS
      PRETTY_NAME: Guardian OS (Atomic)

  - type: initramfs
