# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: guardian-os
description: The Final Guardian Workstation (Qtile, Niri, Hypr, MangoWM)
base-image: quay.io/fedora-ostree-desktops/cosmic-atomic
image-version: 44

modules:
  # STEP 1: Basic Bling (RPM Fusion)
  - type: bling
    install:
      - rpmfusion

  # STEP 2: Core Desktop Suite
  - type: rpm-ostree
    repos:
      - https://github.com/terrapkg/subatomic-repos/raw/main/terra.repo
      - https://copr.fedorainfracloud.org/coprs/yalter/niri/repo/fedora-44/yalter-niri-fedora-44.repo
      - https://copr.fedorainfracloud.org/coprs/solopasha/hyprland/repo/fedora-44/solopasha-hyprland-fedora-44.repo
      - https://copr.fedorainfracloud.org/coprs/lilay/topgrade/repo/fedora-44/lilay-topgrade-fedora-44.repo
    install:
      - mangowm
      - niri
      - hyprland
      - qtile-wayland
      - qtile-extras
      - noctalia-shell
      - topgrade
      - gamescope
      - mangohud
      - gamemode
      - kitty
      - fish
      - neovim
      - xdg-desktop-portal-hyprland
      - xdg-desktop-portal-gtk
      - jq
      - socat

  # STEP 3: Chezmoi
  - type: chezmoi
    repository: "https://github.com/timothybear11/dotfiles"
    all-users: true

  # STEP 4: System Styling & Cleanup
  - type: script
    snippets:
      - "curl -fsSL https://install.danklinux.com | sh"
      - "rm -f /usr/share/dbus-1/services/fr.emersion.mako.service"
      - "echo 'Setting Plymouth theme...' && plymouth-set-default-theme guardian"

  # STEP 5: Assets (Matching your repo structure)
  - type: files
    files:
      - source: system-fonts
        destination: /usr/share/fonts/guardian-fonts
      - source: etc/plymouth
        destination: /etc/plymouth
      - source: usr/share/plymouth/themes/guardian
        destination: /usr/share/plymouth/themes/guardian

  # STEP 6: Rebrand & Rebuild Initramfs
  - type: os-release
    properties:
      NAME: Guardian OS
      ID: guardian_os
      VARIANT_ID: cosmic

  - type: initramfs # This is the "secret sauce" to make Plymouth work early
