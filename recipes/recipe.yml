---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: guardian-os
description: Atomic Qtile + COSMIC workstation
base-image: quay.io/fedora-ostree-desktops/cosmic-atomic
image-version: 43

modules:
  - type: rpm-ostree
    repos:
      - https://rpmfusion.org/keys/rpmfusion-free-fedora.asc
    install:
      - steam
      - distrobox
      - qtile
      - qtile-extras   # Added right here!
      - kitty
      - fish
      - neovim

  - type: brew

  - type: files
    source: config
    destination: /etc/skel/.config

  - type: files
    source: system-fonts                     # The new folder in your repo
    destination: /etc/skel/.local/share/fonts # The native Linux font folder
