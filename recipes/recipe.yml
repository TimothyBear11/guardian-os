---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: guardian-os
description: Atomic Qtile + COSMIC workstation
base-image: quay.io/fedora-ostree-desktops/cosmic-atomic
image-version: 43

modules:
  - type: rpm-ostree
    repos:
      - https://rpmfusion.org/keys/rpmfusion-free-fedora.asc
    install:
      - steam
      - distrobox
      - qtile
      - qtile-extras
      - kitty
      - fish
      - neovim

  - type: brew

  - type: files
    source: files/config    # <--- Updated here
    destination: /etc/skel/.config

  - type: files
    source: files/system-fonts   # <--- Updated here
    destination: /etc/skel/.local/share/fonts
